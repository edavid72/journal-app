import { createTheme } from '@mui/material/styles';

const colorTheme = createTheme({
  palette: {
    primary: {
      main: '#54BAB9',
    },
    secondary: {
      main: '#18978F',
    },
    error: {
      main: '#FF4A4A',
    },
    white: {
      main: '#EEEEEE',
    },
  },
});

export default colorTheme;
